services:
  frontend:
    image: node:22-alpine
    working_dir: /app
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    command: sh -c "npm install && npm run dev -- --host"

  # backend:
  #   build:
  #     context: ./backend
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     - database
  #   env_file:
  #     - ./backend/.env

  # database:
  #   image: mysql:8
  #   environment:
  #     MYSQL_DATABASE: etp_systems
  #     MYSQL_ROOT_PASSWORD: root
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - db_data:/var/lib/mysql

# volumes:
#   db_data:
